// This file is automatically generated by next-pwa
// Custom service worker logic can be added here if needed

self.addEventListener("install", (event) => {
  console.log("WellPal PWA Service Worker installing...")
  self.skipWaiting()
})

self.addEventListener("activate", (event) => {
  console.log("WellPal PWA Service Worker activating...")
  event.waitUntil(self.clients.claim())
})

self.addEventListener("fetch", (event) => {
  // Custom fetch handling can be added here
  // For now, let next-pwa handle all fetch events
})